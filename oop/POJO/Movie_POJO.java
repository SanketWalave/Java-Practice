/*Question 3: Sorting Objects Without Collections

Create a POJO class named Movie with the following fields:

    int movieId
    String movieName
    double rating

Write a program to:

    Create an array of Movie objects.
    Sort the movies based on their rating in descending order using a simple sorting algorithm.
    Display the sorted list of movies.
*/
import java.util.*;
public class Movie_POJO
{
	public static void main(String x[])
	{
		Scanner sc=new Scanner(System.in);
		System.out.println("Enter array size");
		int size=sc.nextInt();
	}
}